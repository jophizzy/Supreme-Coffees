//Using  id: any, if for objects

export interface Coffee {
    id: any;
    uid: string;
    blend_name: string;
    origin: string;
    variety: string;
    notes: string;
    intensifier: string;
}